name: Testing CICD

on:
  push:
    branches:
      - develop

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@main

      - uses: docker/setup-buildx-action@master

      - uses: docker/login-action@master
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - uses: docker/build-push-action@master
        with:
          push: true
          tags: unaplauso/api-gateway:latest
          target: unaplauso-gateway

      - uses: docker/build-push-action@master
        with:
          push: true
          tags: unaplauso/api-auth:latest
          target: unaplauso-auth

      - uses: docker/build-push-action@master
        with:
          push: true
          tags: unaplauso/api-audit:latest
          target: unaplauso-audit

      - uses: docker/build-push-action@master
        with:
          push: true
          tags: unaplauso/api-file:latest
          target: unaplauso-file

      - uses: appleboy/ssh-action@master
        with:
          script: deploy
          command_timeout: '2m'
          username: ec2-user
          host: ${{ secrets.SSH_TESTING_HOST }}
          key: ${{ secrets.SSH_ROOT_KEY }}
