declare HAS_APPS_DIFF=$(git show --name-only -r --stat --oneline HEAD^^..HEAD | grep 'apps/')
declare HAS_LIBS_DIFF=$(git show --name-only -r --stat --oneline HEAD^^..HEAD | grep 'libs/')

if [[ $HAS_APPS_DIFF || $HAS_LIBS_DIFF  ]]; then 
  pnpm format
  pnpm lint
fi
